/* eslint-disable @typescript-eslint/no-unused-vars */
const idNumber = (n: number) => n;

const idString = (n: string) => n;

const idBoolean = (n: number) => n;

const idArray = (n: unknown[]) => n;

const idObject = (n: unknown) => n;

const id = <T>(x: T) => x;
